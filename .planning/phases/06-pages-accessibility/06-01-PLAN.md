---
phase: 06-pages-accessibility
plan: 01
type: execute
wave: 1
depends_on: ["06-00"]
files_modified:
  - frontend/src/app/(marketing)/page.tsx
autonomous: true
requirements:
  - PAGE-01
user_setup: []
must_haves:
  truths:
    - "User sees product name and headline"
    - "User sees product value proposition"
    - "User sees feature cards with icons"
    - "User can click 'Enter Console' button"
    - "User navigates to console page"
  artifacts:
    - path: "frontend/src/app/(marketing)/page.tsx"
      provides: "Marketing home page"
      min_lines: 40
    - path: "frontend/tests/integration/home-page.test.tsx"
      provides: "Integration tests"
      exports: ["describe('Home Page Integration (PAGE-01)"]
  key_links:
    - from: "frontend/src/app/(marketing)/page.tsx"
      to: "/console"
      via: "Link href='/console'"
      pattern: "href=['\"]console"
---

<objective>
Rewrite the home page with semantic HTML, accessible components, and a professional product showcase.

Purpose: Transform the minimal home page into a polished, professional landing page that showcases product value and provides clear entry to the console.

Output: New marketing page with hero, features, and CTA button
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pages-accessibility/06-RESEARCH.md
@.planning/phases/06-pages-accessibility/06-VALIDATION.md

</context>

<interfaces>
<!-- Key types from console components (used for understanding) -->
From frontend/src/components/console:
```typescript
export function EndpointSelector(): void
export function MessageInput(): void
export function ModelJsonPanel(): void
export function ConfigPanel(): void
export function ExecuteButton(): void
export function StatusIndicator({ state }: { state: ConnectionState }): void
export function ErrorDisplay({ error }: { error?: string | null }): void
export function ClarificationPrompt({ clarification }): void
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite home page with semantic structure</name>
  <files>frontend/src/app/(marketing)/page.tsx</files>
  <behavior>
    - Test 1: Home page renders with main landmark
    - Test 2: Home page has h1 with proper id
    - Test 3: Home page has sections with aria-labelledby
    - Test 4: Home page renders feature cards
    - Test 5: CTA button links to console
  </behavior>
  <action>
Rewrite the home page with the structure:

**Semantic structure:**
- Use `<main>` landmark element
- Hero section with `aria-labelledby="hero-heading"`
- `<h1 id="hero-heading">` for main headline
- Features section with `aria-labelledby="features-heading"`
- Proper `<section>` elements

**Content:**
- Hero: Headline "StructureClaw", subheading with product value
- Value proposition: "AI-powered structural engineering workbench"
- Features grid: 3 cards with icons (Zap, Shield, FileText)
- CTA: "Enter Console" button linking to /console

**Accessibility:**
- Add `aria-label="Enter the Agent Console"` to CTA button
- Add `aria-hidden="true"` to decorative icons

**Styling:**
- Use existing design system (Tailwind + CSS variables)
- Responsive layout with `md:grid-cols-3` for features
- Card components for feature cards
- Button size="lg" for primary CTA

**Chinese text:** Keep existing Chinese description for consistency

Do NOT add:
- Complex animations (simple hover/active states already work)
- Authentication or user settings
- Footer (keep minimal - just features grid)
- Multiple CTAs (one primary action)
</action>
  <verify>
    <automated>cd frontend && npm test -- tests/integration/home-page.test.tsx</automated>
  </verify>
  <done>
Home page has semantic HTML structure with hero section, features grid, and accessible CTA button
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration tests for home page</name>
  <files>frontend/tests/integration/home-page.test.tsx</files>
  <behavior>
    - Test 1: Page renders main landmark
    - Test 2: h1 has correct id for aria-labelledby
    - Test 3: CTA link navigates to /console
    - Test 4: Feature cards render with icons
    - Test 5: All interactive elements are keyboard accessible
  </behavior>
  <action>
Implement integration tests for the new home page structure.

**Tests to implement:**
1. Page renders with `<main>` element
2. Hero section exists with `aria-labelledby="hero-heading"`
3. `<h1>` has `id="hero-heading"` matching the aria-labelledby
4. Features section exists with `aria-labelledby="features-heading"`
5. Feature cards render with icons and descriptive text
6. CTA button is a link pointing to `/console`
7. All buttons are reachable via keyboard

**Test setup:**
- Use existing test patterns from console-page.test.tsx
- Use userEvent.setup() for keyboard interaction tests
- Mock Next.js router if needed

</action>
  <verify>
    <automated>cd frontend && npm test -- tests/integration/home-page.test.tsx</automated>
  </verify>
  <done>
All home page integration tests pass with semantic structure and keyboard navigation verified
</done>
</task>

</tasks>

<verification>
- Home page has semantic HTML structure (main, section, h1, h2, landmarks)
- All interactive elements are keyboard accessible
- Integration tests pass
</verification>

<success_criteria>
- Home page renders with hero section, features grid, and CTA button
- All PAGE-01 tests pass
- User can navigate to console via keyboard
</success_criteria>

<output>
After completion, create `.planning/phases/06-pages-accessibility/06-01-SUMMARY.md`
</output>
