---
phase: 06-pages-accessibility
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/tests/integration/home-page.test.tsx
  - frontend/tests/accessibility/keyboard.test.tsx
  - frontend/tests/accessibility/focus-management.test.tsx
  - frontend/tests/accessibility/semantic.test.tsx
  - frontend/tests/accessibility/aria-labels.test.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Test files exist for all Wave 0 requirements"
    - "Each test file has it.todo() stubs for planned tests"
  artifacts:
    - path: "frontend/tests/integration/home-page.test.tsx"
      provides: "Test stubs for PAGE-01"
      min_lines: 20
    - path: "frontend/tests/accessibility/keyboard.test.tsx"
      provides: "Test stubs for ACCS-01"
      min_lines: 20
    - path: "frontend/tests/accessibility/focus-management.test.tsx"
      provides: "Test stubs for ACCS-02"
      min_lines: 20
    - path: "frontend/tests/accessibility/semantic.test.tsx"
      provides: "Test stubs for ACCS-03"
      min_lines: 20
    - path: "frontend/tests/accessibility/aria-labels.test.tsx"
      provides: "Test stubs for ACCS-04"
      min_lines: 20
  key_links: []
---

<objective>
Create test infrastructure stubs for Phase 6 accessibility validation.

Purpose: Enable TDD workflow for pages and accessibility requirements.
Output: Test files with it.todo() stubs for all requirements.
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pages-accessibility/06-VALIDATION.md

# Pattern from prior phases
Test stubs use it.todo() pattern for planned but unimplemented tests.
Describe blocks include requirement ID for traceability: 'Feature Name (REQ-ID)'
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create accessibility test directory structure</name>
  <files>frontend/tests/accessibility/.gitkeep</files>
  <action>Create the tests/accessibility directory with a .gitkeep file to ensure the directory exists in git.</action>
  <verify>
    <automated>test -d frontend/tests/accessibility && echo "Directory exists"</automated>
  </verify>
  <done>Accessibility test directory ready for test files</done>
</task>

<task type="auto">
  <name>Task 2: Create home page integration test stubs</name>
  <files>frontend/tests/integration/home-page.test.tsx</files>
  <action>Create test stubs for PAGE-01 home page validation. Include it.todo() stubs for:
- Home page renders with hero section
- Home page has proper heading hierarchy (h1 for title)
- Home page has semantic landmarks (main, section)
- CTA button links to /console
- All interactive elements have proper ARIA labels

Use describe block format: 'Home Page (PAGE-01)'</action>
  <verify>
    <automated>cd frontend && npm test -- tests/integration/home-page.test.tsx --passWithNoTests 2>&1 | grep -E "(PASS|it.todo|Home Page)"</automated>
  </verify>
  <done>Test stubs ready for PAGE-01 implementation</done>
</task>

<task type="auto">
  <name>Task 3: Create keyboard navigation test stubs</name>
  <files>frontend/tests/accessibility/keyboard.test.tsx</files>
  <action>Create test stubs for ACCS-01 keyboard navigation. Include it.todo() stubs for:
- All buttons on home page are reachable via Tab
- All buttons on console page are reachable via Tab
- Form inputs are reachable via Tab
- Select dropdowns are operable via keyboard
- Dialog can be opened and closed with Enter/Escape
- Tab order follows visual layout

Use describe block format: 'Keyboard Navigation (ACCS-01)'</action>
  <verify>
    <automated>cd frontend && npm test -- tests/accessibility/keyboard.test.tsx --passWithNoTests 2>&1 | grep -E "(PASS|it.todo|Keyboard Navigation)"</automated>
  </verify>
  <done>Test stubs ready for ACCS-01 implementation</done>
</task>

<task type="auto">
  <name>Task 4: Create focus management test stubs</name>
  <files>frontend/tests/accessibility/focus-management.test.tsx</files>
  <action>Create test stubs for ACCS-02 focus management. Include it.todo() stubs for:
- Focus is trapped within open dialogs
- Focus returns to trigger element when dialog closes
- Error display receives focus when error appears
- Clarification prompt receives focus when it appears
- Dynamic content uses aria-live for announcements

Use describe block format: 'Focus Management (ACCS-02)'

Note: Some tests already exist in tests/components/dialog.test.tsx for Dialog focus trap - reference those patterns.</action>
  <verify>
    <automated>cd frontend && npm test -- tests/accessibility/focus-management.test.tsx --passWithNoTests 2>&1 | grep -E "(PASS|it.todo|Focus Management)"</automated>
  </verify>
  <done>Test stubs ready for ACCS-02 implementation</done>
</task>

<task type="auto">
  <name>Task 5: Create semantic HTML test stubs</name>
  <files>frontend/tests/accessibility/semantic.test.tsx</files>
  <action>Create test stubs for ACCS-03 semantic HTML. Include it.todo() stubs for:
- Home page has main landmark
- Console page has main landmark
- Pages use proper heading hierarchy (h1 > h2 > h3)
- Navigation uses nav element with aria-label
- Form controls have associated labels
- Buttons use button element (not div with onClick)

Use describe block format: 'Semantic HTML (ACCS-03)'</action>
  <verify>
    <automated>cd frontend && npm test -- tests/accessibility/semantic.test.tsx --passWithNoTests 2>&1 | grep -E "(PASS|it.todo|Semantic HTML)"</automated>
  </verify>
  <done>Test stubs ready for ACCS-03 implementation</done>
</task>

<task type="auto">
  <name>Task 6: Create ARIA labels test stubs</name>
  <files>frontend/tests/accessibility/aria-labels.test.tsx</files>
  <action>Create test stubs for ACCS-04 ARIA labels. Include it.todo() stubs for:
- Icon-only buttons have aria-label
- Form inputs have accessible names (via label or aria-label)
- Select triggers have aria-label
- Links have descriptive text (not "click here")
- Dynamic content has aria-live attributes
- Error messages have role="alert"

Use describe block format: 'ARIA Labels (ACCS-04)'</action>
  <verify>
    <automated>cd frontend && npm test -- tests/accessibility/aria-labels.test.tsx --passWithNoTests 2>&1 | grep -E "(PASS|it.todo|ARIA Labels)"</automated>
  </verify>
  <done>Test stubs ready for ACCS-04 implementation</done>
</task>

</tasks>

<verification>
- All test files exist in frontend/tests/
- Each file has describe blocks with it.todo() stubs
- Run `cd frontend && npm test` to verify stub syntax is valid
</verification>

<success_criteria>
- 5 test stub files created with it.todo() patterns
- All files pass syntax validation (no TypeScript errors)
- Directory structure matches VALIDATION.md requirements
</success_criteria>

<output>
After completion, create `.planning/phases/06-pages-accessibility/06-00-SUMMARY.md`
</output>
