---
phase: 06-pages-accessibility
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - frontend/src/components/console/execute-button.tsx
  - frontend/src/components/layout/split-panel.tsx
autonomous: true
requirements:
  - ACCS-01
user_setup: []
must_haves:
  truths:
    - "All buttons on console page are reachable via Tab key"
    - "Tab order follows visual layout"
    - "Execute button is active when focused"
    - "Focus returns to dialog after it closes"
  artifacts:
    - path: "frontend/tests/accessibility/keyboard.test.tsx"
      provides: "Keyboard navigation tests"
      min_lines: 40
  key_links:
    - from: "frontend/tests/accessibility/keyboard.test.tsx"
      to: "Console page components"
      via: "rendered component tree"
      pattern: "screen.getByRole.*"
---
<objective>
Verify keyboard navigation across all console page interactive elements.

Purpose: Ensure all interactive elements are keyboard accessible per WCAG 2.1.1 (keyboard) and 2.1.2 (no keyboard trap).

Output: Keyboard navigation tests that verify accessible console components
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pages-accessibility/06-RESEARCH.md

@.planning/phases/06-pages-accessibility/06-VALIDATION.md
</context>

<interfaces>
<!-- Existing console components with their keyboard handling -->
From frontend/src/components/console/EndpointSelector:
- Uses Radix Select with built-in keyboard navigation
- Uses Button for execute action

From frontend/src/components/console/MessageInput:
- Has form inputs with proper labels

From frontend/src/components/console/ExecuteButton:
- Uses Button component, extends={size, variants}, from shadcn/ui
- Focus management via Radix Dialog already handled
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test keyboard navigation on console page</name>
  <files>frontend/tests/accessibility/keyboard.test.tsx</files>
  <behavior>
    - Test 1: Tab reaches all buttons in left panel
    - Test 2: Tab order follows visual layout (left to right)
    - Test 3: Tab reaches all form controls
    - Test 4: Execute button responds to keyboard (Enter/Space)
    - Test 5: Dialog keyboard patterns work (Escape to close)
  </behavior>
  <action>
Implement comprehensive keyboard navigation tests for the console page.

**Tests to implement:**
1. **Tab order verification:** All interactive elements reachable via Tab key
   - Render console page
   - Get all buttons (screen.getAllByRole('button'))
   - Simulate Tab navigation and verify button order
   - Verify order follows visual layout (left panel: EndpointSelector, MessageInput, ModelJsonPanel, ConfigPanel, then ExecuteButton)

2. **Form control keyboard interaction:**
   - Get all form controls (screen.getAllByRole('combobox') for Select, 'input' for Input, 'textarea' for Textarea)
   - Verify each is reachable via Tab
   - Verify first Tab reaches the first form control
   - Verify subsequent tabs reach remaining controls in order

3. **Button keyboard interaction:**
   - Get ExecuteButton
   - Focus it
   - await user.keyboard('{Enter}')
   - Verify it triggers submit or onClick is called
   - Verify aria-label exists

4. **Modal behavior:**
   - Render Dialog component
   - Open it
   - Verify Escape closes it
   - Verify focus is trapped within dialog
   - Close dialog
   - Verify focus returns to trigger button

</action>
  <verify>
    <automated>cd frontend && npm test -- tests/accessibility/keyboard.test.tsx</automated>
  </verify>
  <done>
All keyboard navigation tests pass for ACCS-01
</done>
</task>

</tasks>

<verification>
- All interactive elements reachable via Tab
- Tab order follows visual layout
- All ACCS-01 tests pass
</verification>

<success_criteria>
- Keyboard navigation verified through tests
- All console components are keyboard accessible
</success_criteria>

<output>
After completion, create `.planning/phases/06-pages-accessibility/06-03-SUMMARY.md`
</output>
